<section id="meal-search-results">
    <div class="row">
        <div class="col-md-8">
            <% if @meals.present? %>
                <%= render partial: 'meals/meals', locals: { meals: @meals } %>
            <% else %>
                <%= render 'meals/no_meal' %>
            <% end %>
        </div>
    </div>
</section>