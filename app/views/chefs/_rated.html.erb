<% 5.times.each_with_index do |s, idx| %>
    <span class="star rated-star">
        <i class="<%= star_rating(idx + 1, @cook) %> theme-cyan"
            style="font-size: 28px;"></i>
    </span>
<% end %>
<span class="rating-count" style="font-size: 24px;">
    <%= @cook.rating_count %>
</span>