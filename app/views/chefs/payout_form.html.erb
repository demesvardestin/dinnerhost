<section id="my-payouts" style="height: 100%;">
    <% if current_chef.has_stripe_account %>
        <%= render 'has_stripe_account' %>
    <% else %>
        <%= render 'payout_form' %>
    <% end %>
</section>