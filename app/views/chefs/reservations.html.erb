<section id="cook-reservations">
    <h3 class="bold">Reservations</h3>
    
    <div id="reservation-controls" style="margin-top: 20px;">
        <%= render 'reservation_controls' %>
    </div>
    
    <div class="row">
        <div class="col-md-12">
            <hr>
        </div>
    </div>
    
    <div class="row" id="reservations-box">
        <%= render partial: 'reservations', locals: { reservations: @reservations.pending } %>
    </div>
</section>

<script type="text/javascript">
    function loadReservationCategory(category, element) {
        loading(element);
        var pending = <%= @reservations.pending.count %>;
        $.get('/reservations/' + category);
        $('.reservation-control-button').html(`
            <span style="padding-right: 45px;">
                ` + category.slice(0, 1).toUpperCase() + category.slice(1) + `
            </span> <i class="fa fa-angle-down"></i>
        `);
    }
</script>