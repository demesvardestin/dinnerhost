toastr.warning("You have denied <%= @reservation.customer.first_name %>'s reservation!");

$('#reservations-box').html(`
    <%= render partial: 'reservations', locals: { reservations: @pending } %>
`);